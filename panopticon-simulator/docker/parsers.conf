# infra/docker/parsers.conf 파일을 새로 생성합니다.
# - StructuredLogger가 출력한 timestamp 필드를 Fluent Bit 시간으로 사용합니다.

[PARSER]
    Name        panopticon_json
    Format      json
    Time_Key    timestamp
    Time_Format %Y-%m-%dT%H:%M:%S.%LZ
    Time_Keep   On

